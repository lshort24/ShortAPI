Login to AWS: aws-new - if it hangs check AWS security group for inbound SSH that your IP is listed

Composer
- You need to add require_once 'vendor/autoload.php' before use statements
- After updating the AWS server, download the changes to composer.json and composer.lock to the project and commit them.

GraphQL
- Use ChromeiQL Chrome extension to send queries to https://shortsrecipes.com/graphql.php

Maintenance:
sudo yum update -y
sudo systemctl restart httpd
sudo systemctl restart mariadb
sudo systemctl restart vsftpd
Verify remote website is working
